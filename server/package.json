{
  "name": "escape-hub-api",
  "version": "1.1.0",
  "private": true,
  "description": "Cloudflare Worker API + D1 database for the Classroom Escape Hub (results collection, teacher auth, custom escapes).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply escape_hub --local",
    "migrate:remote": "wrangler d1 migrations apply escape_hub --remote"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
